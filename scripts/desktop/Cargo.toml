[package]
name = "askuser-desktop"
version = "0.1.0"
edition = "2021"
description = "janela always-on-top que abre a tela do askuser"

[[bin]]
name = "askuser-desktop"
path = "src/main.rs"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
